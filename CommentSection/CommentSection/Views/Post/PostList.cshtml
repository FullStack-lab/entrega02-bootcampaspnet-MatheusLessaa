@using CommentSection.Models.Comment
@model List<Posted>

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta viewport ="width=device-width"/>
</head>

<body>
    <h3>Aqui ficam os comentários do post</h3>




    @foreach (var comment in Model)
    {
        @comment.Description;
        @comment.Author;
        <a href="@Url.Action("Details", "Post", new { id = comment.Id })">
            Ver detalhes
        </a>
        <p>
            @comment.ReplyList.Count Respostas
        </p>
        <hr />
    }

    <button type="button" class="btn btn-primary" onclick="window.location.href='@Url.Action("Create", "Post")'">
        Novo Comentário
    </button>
</body>
</html>